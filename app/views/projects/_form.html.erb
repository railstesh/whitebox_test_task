<%# <%= form_with(model: project) do |form| %> 
<%# <%= form_for [@project.user, @project] do |form| %> 
<%= form_for @project, url: [:user, @project] do |form| %>
  <% if project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row g-3 align-items-center">
     <div class="col-auto">
    <%= form.label :title, style: "display: block" , class:"col-form-label"%>
    <%= form.text_field :title %>
     </div>

  <div class="col-auto">
    <%= form.label :description, style: "display: block" ,class:"col-form-label"%>
    <%= form.text_field :description %>
  </div>

   <div class="col-auto">
    <%= form.label :start_date, style: "display: block" ,class:"col-form-label"%>
    <%= form.date_field :start_date %>
  </div>

  <div  class="col-auto">
    <%= form.label :end_date, style: "display: block", class:"col-form-label" %>
    <%= form.date_field :end_date %>
  </div>

  <div class="col-auto">
    <%= form.label :status, style: "display: block" , class:"col-form-label"%>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
